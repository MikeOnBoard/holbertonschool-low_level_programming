#include "main.h"

/** Anotaciones para este programa
 * realizaremos una multiplicacion
 * al tratarse de dos enteros y que el final de la declaracion de la funcion prototipo(tipo entero).
 * al final simplemente devolveremos el resultado de la multiplicacion.
 * el resultado sera entregado por esta funcion pero los valores de la funcion seran entregados por la funcion main que llama a esta funcion dentro del programa. 
 */


int mul(int a, int b)
{
    int mul = a * b; 
    return(mul);
}